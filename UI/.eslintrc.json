{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "@/lib/prisma",
            "message": "Please use repositories in '@/server/repositories/' instead of direct Prisma access. prisma should only be imported within the server/ directory."
          }
        ],
        "patterns": [
          {
            "group": ["**/lib/prisma"],
            "message": "Please use repositories in '@/server/repositories/' instead of direct Prisma access. prisma should only be imported within the server/ directory."
          }
        ]
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/server/**/*.ts", "src/lib/prisma.ts", "src/__tests__/**/*.ts", "src/pages/api/**/*.ts"],
      "rules": {
        "no-restricted-imports": "off"
      }
    }
  ]
}
