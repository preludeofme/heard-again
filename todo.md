[x] move search in family tree to inside the toolbar
[x] I want to redesign the nodes 
    [x] need to use a single design (i like the simple compact design with just the icons and no labels)
    [x] Colors (5 different branded colors needed)
        [x] Selected family member should be a unique color
        [x] Self node for the user should always been a unique color
        [x] each level needs to be a different color (colors need to match the theme and branding), the colors should repeat after 3 levels so just 3 different colors needed
[x] increase the token expiration to 1 day
[x] the family tree node modal view does not filter the stories by the user, i am on aurora buck which has 0 stories and it's showing stories for ryan buck
