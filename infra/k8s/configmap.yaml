apiVersion: v1
kind: ConfigMap
metadata:
  name: heard-again-config
  namespace: heard-again
data:
  # Update to match Terraform output: tts_models_bucket
  # Default follows naming convention: {app_name}-{environment}-tts-models
  tts-models-bucket: "heard-again-prod-tts-models"
