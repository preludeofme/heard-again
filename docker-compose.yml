version: '3.8'

services:
  # Chat System — internal only, not reachable from the host
  chat:
    build:
      context: ./Chat
      dockerfile: docker/Dockerfile
    # No public port binding — chat is accessed only via the internal network
    expose:
      - "4778"
    environment:
      - NODE_ENV=production
      - PORT=4778
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/heard_again
      - REDIS_URL=redis://redis:6379
      - CHROMA_URL=http://chromadb:8000
      - OLLAMA_URL=http://ollama:11434
      - TTS_SERVICE_URL=http://tts:4779
      - CHAT_SERVICE_SECRET=${CHAT_SERVICE_SECRET:?CHAT_SERVICE_SECRET must be set}
    networks:
      - internal
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      chromadb:
        condition: service_healthy
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  # Ollama LLM Service (optional - requires GPU) — internal only
  ollama:
    image: ollama/ollama:latest
    # No public port binding
    expose:
      - "11434"
    networks:
      - internal
    volumes:
      - ollama_data:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    profiles:
      - with-llm

  # Main Next.js application
  app:
    build:
      context: .
      dockerfile: UI/Dockerfile
    ports:
      - "4777:4777"
    environment:
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/heard_again
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-change-this-in-production}
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:4777}
      - TTS_SERVICE_URL=${TTS_SERVICE_URL:-http://tts:4779}
      - CHAT_SYSTEM_URL=${CHAT_SYSTEM_URL:-http://chat:4778}
      - CHAT_SERVICE_SECRET=${CHAT_SERVICE_SECRET:?CHAT_SERVICE_SECRET must be set}
      - TTS_SERVICE_TOKEN=${TTS_SERVICE_TOKEN:?TTS_SERVICE_TOKEN must be set}
      - REDIS_URL=redis://redis:6379
      - CLAMAV_HOST=clamav
      - CLAMAV_PORT=3310
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - REQUIRE_SIGNED_URLS=true
      - NODE_ENV=production
    networks:
      - internal
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      clamav:
        condition: service_healthy
    volumes:
      - uploads:/app/uploads
      - generated:/app/generated
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=100m
      - /app/tmp:noexec,nosuid,size=100m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4777/api/instance/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # PostgreSQL database
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set (min 24 chars)}
      - POSTGRES_DB=heard_again
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ClamAV antivirus service (mandatory for production)
  clamav:
    image: clamav/clamav:stable
    container_name: clamav
    restart: unless-stopped
    environment:
      - CLAMAV_NO_CLAMD=false
      - CLAMAV_NO_FRESHCLAN=false
    volumes:
      - clamav_db:/var/lib/clamav
      - /var/run/clamav:/var/run/clamav
    networks:
      - internal
    ports:
      - "3310:3310"
    healthcheck:
      test: ["CMD-SHELL", "clamdscan --ping || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # Redis for distributed rate limiting and caching
  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # TTS Service (optional - requires GPU)
  tts:
    build:
      context: ./TTS
      dockerfile: Dockerfile
    expose:
      - "4779"
    environment:
      - PORT=4779
      - MODEL_DEVICE=cuda
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
      - STAGING_URL=${STAGING_URL}
    volumes:
      - tts_models:/app/models
      - ./TTS/generated:/app/generated
    networks:
      - internal
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    profiles:
      - with-tts

  # ChromaDB for vector search (RAG) — internal only, not reachable from the host
  chromadb:
    image: chromadb/chroma:latest
    # No public port binding — only the chat service needs to reach chromadb
    expose:
      - "8000"
    environment:
      - IS_PERSISTENT=TRUE
      - CHROMA_SERVER_HOST=0.0.0.0
      - CHROMA_SERVER_PORT=8000
      - ALLOW_RESET=FALSE
      - ANONYMIZED_TELEMETRY=FALSE
    volumes:
      - chroma_data:/chroma/chroma
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

  # Ingestion Worker for document processing
  ingestion-worker:
    build:
      context: ./Chat
      dockerfile: docker/Dockerfile.worker
    # No public port binding - health check is internal
    expose:
      - "4779"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/heard_again
      - REDIS_URL=redis://redis:6379
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CHROMA_URL=http://chromadb:8000
      - OLLAMA_URL=http://ollama:11434
      - CHAT_SERVICE_SECRET=${CHAT_SERVICE_SECRET:?CHAT_SERVICE_SECRET must be set}
      - PORT=4779  # Worker health check port
    networks:
      - internal
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      chromadb:
        condition: service_healthy
      ollama:
        condition: service_healthy
    volumes:
      - uploads:/app/uploads
      - temp_ingestion:/app/temp-ingestion
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4779/api/worker/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    profiles:
      - with-ingestion

networks:
  internal:
    driver: bridge

volumes:
  postgres_data:
  uploads:
  generated:
  tts_models:
  redis_data:
  clamav_db:
  chroma_data:
  ollama_data:
  temp_ingestion:
